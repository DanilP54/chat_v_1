import {render} from "@testing-library/react";
import { StatusToggle } from "./status.toggle";

describe('StatusToggle', () => {
    it('should render', () => {
        render(<StatusToggle />)
    })
})

